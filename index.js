function takeANumber(num, name) {
  var line = num.length + 1;
  return `Welcome, ${name}. You are number ${line} in line.`
}

function nowServing(num) {
  
}

function currentLine(num) {
  return `The line is currently: `
}